{
  "name": "grainfall",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Rule a city-state one year at a time — split the grain, sow the fields, read the weather, and keep your people alive.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/gen-icons.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
